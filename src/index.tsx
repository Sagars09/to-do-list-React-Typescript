import * as React from 'react';
import * as ReactDOM from 'react-dom';

import {Form, AddForm} from "./components/Form";

ReactDOM.render (
    <div>
        <Form/>
    </div>,
    document.getElementById('form')
);