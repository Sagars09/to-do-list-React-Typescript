import * as React from 'react';
interface Add {
    id: number,
    text: string,
    check: boolean
}

interface AddProps{
    items: Add[];
}

const addItem = (props: AddProps) => {
    <div>
        
    </div>
};